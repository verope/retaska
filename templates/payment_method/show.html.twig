{% extends 'base.html.twig' %}

{% block title %}PaymentMethod{% endblock %}

{% block body %}
    <h1>PaymentMethod</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ payment_method.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ payment_method.type }}</td>
            </tr>
            <tr>
                <th>Surcharge</th>
                <td>{{ payment_method.surcharge }}</td>
            </tr>
            <tr>
                <th>PriceEur</th>
                <td>{{ payment_method.priceEur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('payment_method_index') }}">back to list</a>

    <a href="{{ path('payment_method_edit', {'id': payment_method.id}) }}">edit</a>

    {{ include('payment_method/_delete_form.html.twig') }}
{% endblock %}
